# IoT.js module: $MODULE_NAME$

## How to build?

In the source directory of the module:

```sh
$ cmake -Bbuild -H.
$ make -C build
```

## How to test?

In the source directory of the module:

```sh
$ iotjs js/test.js
```
